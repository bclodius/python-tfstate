language: python
python:
    - "3.4"
    - "3.5"
install:
    - "python setup.py install"
    - "pip install -r requirements/tests.txt"
script:
    - "nosetests -v unit_tests"
    - "nosetests -v functional_tests"
